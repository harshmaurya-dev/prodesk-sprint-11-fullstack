import { useState } from "react";
export default function ItemForm({onSubmit,submitting}){const[title,setTitle]=useState("");const[description,setDescription]=useState("");const[image,setImage]=useState(null);const handleSubmit=async(e)=>{e.preventDefault();const formData=new FormData();formData.append("title",title);formData.append("description",description);if(image)formData.append("image",image);const success=await onSubmit(formData);if(success){setTitle("");setDescription("");setImage(null);e.target.reset()}};return <form className="panel form-panel" onSubmit={handleSubmit}><div><span className="eyebrow">POST REQUEST</span><h2>Add New Entry</h2><p className="muted">This form sends multipart FormData to the Express backend.</p></div><label>Title<input value={title} onChange={e=>setTitle(e.target.value)} placeholder="e.g. Sprint 11 Integration" maxLength={100} required/></label><label>Description<textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder="Describe the entry..." maxLength={500} rows={5} required/></label><label>Thumbnail Image<input type="file" accept="image/*" onChange={e=>setImage(e.target.files?.[0]||null)}/></label><button className="primary-button" disabled={submitting}>{submitting?"Saving...":"Save to MongoDB"}</button></form>}
