export default function ItemList({items,onDelete}){if(!items.length)return <div className="empty-state"><h3>No entries yet</h3><p>Create your first entry using the form.</p></div>;return <div className="item-list">{items.map(item=><article className="item-card" key={item._id}>{item.imageUrl?<img src={item.imageUrl} alt={item.title}/>:<div className="image-placeholder">No Image</div>}<div className="item-body"><div><h3>{item.title}</h3><p>{item.description}</p></div><div className="item-footer"><small>{item.createdAt?new Date(item.createdAt).toLocaleString():"Saved"}</small><button className="delete-button" onClick={()=>onDelete(item._id)}>Delete</button></div></div></article>)}</div>}
